---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openldap-pvc
  namespace: authentication
spec:
  storageClassName: longhorn-low-capacity # use only low capacity nodes
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
