---
apiVersion: v1
kind: Service
metadata:
  name: wordpress
  namespace: blog-test
  labels:
    app: wordpress
spec:
  type: ClusterIP
  ports:
    - name: "http"
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: wordpress
    tier: frontend
