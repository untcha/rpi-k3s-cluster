---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mariadb-data
  namespace: blog2-test
  labels:
    app: mariadb
  annotations:
    nfs.io/storage-path: "/mariadb"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
