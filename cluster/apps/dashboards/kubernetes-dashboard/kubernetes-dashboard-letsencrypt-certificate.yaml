---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kubernetes-dashboard-letsencrypt-production-certificate
  namespace: kubernetes-dashboard
spec:
  commonName: kubernetes.${SECRET_DIGITALOCEAN_DOMAIN_02}
  secretName: kubernetes-dashboard-letsencrypt-production-certificate
  dnsNames:
    - kubernetes.${SECRET_DIGITALOCEAN_DOMAIN_02}
  issuerRef:
    name: letsencrypt-dns01-production-do
    kind: ClusterIssuer
