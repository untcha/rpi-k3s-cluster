---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-postgres-pvc
  namespace: dev-gitea
spec:
  storageClassName: longhorn-high-capacity # use only high capacity nodes
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
