---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-redis-pvc
  namespace: dev-gitea
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
