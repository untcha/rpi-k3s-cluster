---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: portainer-letsencrypt-production-certificate
  namespace: dev-portainer
spec:
  commonName: portainer.${SECRET_DIGITALOCEAN_DOMAIN_02}
  secretName: portainer-letsencrypt-production-certificate
  dnsNames:
    - portainer.${SECRET_DIGITALOCEAN_DOMAIN_02}
  issuerRef:
    name: letsencrypt-dns01-production-do
    kind: ClusterIssuer
