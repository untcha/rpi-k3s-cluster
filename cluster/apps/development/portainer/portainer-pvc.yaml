---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: portainer-data-pvc
  namespace: portainer
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-high-capacity
  resources:
    requests:
      storage: 8Gi
