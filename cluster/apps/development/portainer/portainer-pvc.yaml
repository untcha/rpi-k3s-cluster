---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: portainer-data-pvc
  namespace: portainer
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-worker-node-storageclass
  resources:
    requests:
      storage: 5Gi
