---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: do-dns-updater-log-pvc
  namespace: dns
spec:
  storageClassName: longhorn-low-capacity
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi
