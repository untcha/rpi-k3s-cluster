---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: pihole-env-vars2
    namespace: dns-staging
    files:
      - pihole-env-vars.yaml
generatorOptions:
  disableNameSuffixHash: true
