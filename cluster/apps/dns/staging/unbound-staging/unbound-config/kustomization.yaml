---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: unbound-config
    namespace: dashboards
    files:
      - unbound.conf
generatorOptions:
  disableNameSuffixHash: true
