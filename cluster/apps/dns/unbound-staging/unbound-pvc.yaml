---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unbound-root-key-pvc
  namespace: dns-test
spec:
  storageClassName: longhorn-low-capacity
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
