---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unbound-root-key-pvc
  namespace: dns
  annotations:
    nfs.io/storage-path: "/unbound"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 128Mi
