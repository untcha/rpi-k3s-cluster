---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docs-static-nginx-pvc
  namespace: docs
  annotations:
    nfs.io/storage-path: "html"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 128Mi
