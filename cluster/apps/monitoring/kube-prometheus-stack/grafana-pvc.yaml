---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kps-grafana-data-pvc
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-high-capacity
  resources:
    requests:
      storage: 8Gi
