apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: speedtest-net-exporter
spec:
  selector:
    matchLabels:
      app: speedtest-net-exporter
  podMetricsEndpoints:
  - port: http-exporter
