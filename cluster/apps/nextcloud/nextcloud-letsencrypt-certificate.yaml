---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nextcloud-letsencrypt-production-certificate
  namespace: nextcloud
spec:
  commonName: cloud.${SECRET_DIGITALOCEAN_DOMAIN_02}
  secretName: nextcloud-letsencrypt-production-certificate
  dnsNames:
    - cloud.${SECRET_DIGITALOCEAN_DOMAIN_02}
  issuerRef:
    name: letsencrypt-dns01-production-do
    kind: ClusterIssuer
