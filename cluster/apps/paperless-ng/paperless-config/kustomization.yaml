---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: paperless-config
    namespace: paperless
    envs:
      - paperless.env

generatorOptions:
  disableNameSuffixHash: true
  labels:
    app: paperless
