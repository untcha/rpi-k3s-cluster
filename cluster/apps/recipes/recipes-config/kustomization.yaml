---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - recipes-configmap.yaml

configMapGenerator:
  - name: recipes-config
    namespace: recipes
    envs:
      - recipes.env
generatorOptions:
  disableNameSuffixHash: true
  labels:
    app: recipes
