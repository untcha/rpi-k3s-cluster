---
apiVersion: v1
kind: Service
metadata:
  name: wordpress-mariadb
  namespace: wordpress-test
  labels:
    app: wordpress
spec:
  selector:
    app: wordpress
    tier: mariadb
  clusterIP: none
  ports:
    - port: 3306
  