---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wordpress-data
  namespace: wordpress-test
  labels:
    app: wordpress
  annotations:
    nfs.io/storage-path: "/wordpress"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
