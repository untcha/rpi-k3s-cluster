apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-provisioner-test-pvc
  annotations:
    nfs.io/storage-path: "/test/test/test"
spec:
  storageClassName: managed-nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
