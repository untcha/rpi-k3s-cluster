---
apiVersion: v1
kind: Service
metadata:
  name: traefik-error-pages
  namespace: traefik
spec:
  selector:
    name: traefik-error-pages
  ports:
  - name: web
    port: 8088
    protocol: TCP
    targetPort: 8080
