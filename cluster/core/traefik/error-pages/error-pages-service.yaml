---
apiVersion: v1
kind: Service
metadata:
  name: traefik-error-pages
  namespace: traefik
spec:
  selector:
    name: traefik-error-pages
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
