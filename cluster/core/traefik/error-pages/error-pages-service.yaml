---
apiVersion: v1
kind: Service
metadata:
  name: traefik-error-pages
  namespace: traefik
spec:
  selector:
    app: traefik-error-pages
  ports:
  - name: http
    port: 8080
    protocol: TCP
  type: ClusterIP
