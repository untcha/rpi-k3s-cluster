---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: traefik-nfs-pvc
  namespace: traefik
spec:
  storageClassName: managed-nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 128Mi
