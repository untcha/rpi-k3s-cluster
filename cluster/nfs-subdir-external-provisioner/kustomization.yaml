apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:

namespace: nfs-subdir-external-provisioner

resources:
  - namespace.yaml
  - rbac.yaml
  - class.yaml
  - deployment.yaml
  - pvc-nfs-pv1.yaml
