---
apiVersion: v1
kind: Service
metadata:
  name: redisinsight-svc
  namespace: dev-tools
  labels:
    app: redisinsight
  annotations:
    metallb.universe.tf/address-pool: services
spec:
  selector:
    app: redisinsight
  ports:
  - name: http
    port: 8001
    protocol: TCP
    targetPort: http
  type: LoadBalancer
